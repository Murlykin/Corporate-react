import { Header, } from "./Header.styled";
import Navbar from "../Nave/index";

const Headerbar = () => {
    return (
        <Header>
            <Navbar />
        </Header>
    );
};

export default Headerbar;